# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
crash.log

# Local control-tool state (contains your AC path, bucket, instance id)
.ac/

# Your live server config(s) — may contain admin passwords. Use
# examples/server.example.yml as the shareable template.
server.yml
server.*.yml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/

# Content staging (never commit copyrighted car/track files)
content-cache/
*.acd
*.kn5

# OS
Thumbs.db
.DS_Store
